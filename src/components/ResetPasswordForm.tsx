"use client";import{FormEvent,useState}from"react";import Link from"next/link";import{CheckCircle2,KeyRound}from"lucide-react";import{RecoveryShell}from"@/components/RecoveryShell";export function ResetPasswordForm({token}:{token:string}){const[error,setError]=useState("");const[done,setDone]=useState(false);const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm"))return setError("Les mots de passe ne correspondent pas.");setLoading(true);const r=await fetch("/api/auth/recovery/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:f.get("password")})});const d=await r.json();setLoading(false);if(!r.ok)return setError(d.error);setDone(true)}return <RecoveryShell title="Nouveau mot de passe" subtitle="Choisissez un mot de passe personnel et sécurisé.">{done?<div className="recovery-success"><CheckCircle2 size={18}/><p>Mot de passe modifié avec succès.</p><Link className="btn btn-primary" href="/connexion">Se connecter</Link></div>:!token?<div className="form-alert">Lien de récupération incomplet.</div>:<form onSubmit={submit}>{error&&<div className="form-alert">{error}</div>}<div className="field"><label>Nouveau mot de passe</label><input className="input" name="password" type="password" minLength={12} required/></div><div className="field"><label>Confirmer le mot de passe</label><input className="input" name="confirm" type="password" minLength={12} required/></div><div className="password-rules"><span>12 caractères minimum</span><span>Majuscule et minuscule</span><span>Chiffre et caractère spécial</span></div><button className="btn btn-primary recovery-submit" disabled={loading}><KeyRound size={14}/>{loading?"Modification…":"Modifier le mot de passe"}</button></form>}</RecoveryShell>}
