"use client";import{Globe2}from"lucide-react";import{useI18n}from"./I18nProvider";export function LanguageSelector({compact=false}:{compact?:boolean}){const{locale,languages,changeLanguage}=useI18n();return <label className={`language-selector ${compact?"compact":""}`}><Globe2 size={16}/><select value={locale} onChange={e=>changeLanguage(e.target.value)} aria-label="Langue de l’interface">{languages.filter(l=>(l as any).active!==false).map(l=><option value={l.code} key={l.code}>{compact?l.code.toUpperCase():l.nativeName}</option>)}</select></label>}
