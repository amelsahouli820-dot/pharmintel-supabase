import Link from"next/link";import{ArrowLeft,ShieldCheck}from"lucide-react";export function RecoveryShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <main className="recovery-page"><section className="recovery-card"><div className="login-icon"><ShieldCheck size={23}/></div><span className="eyebrow">RÉCUPÉRATION SÉCURISÉE</span><h1>{title}</h1><p>{subtitle}</p>{children}<Link className="back-login" href="/connexion"><ArrowLeft size={13}/> Retour à la connexion</Link></section></main>}
