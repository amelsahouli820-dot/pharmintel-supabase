"use client";import{FormEvent,useState}from"react";import{LifeBuoy}from"lucide-react";import{RecoveryShell}from"@/components/RecoveryShell";export default function Page(){const[message,setMessage]=useState("");const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/support",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(!r.ok)return setError(d.error);setMessage(d.message)}return <RecoveryShell title="Contacter l’administrateur" subtitle="Utilisez ce formulaire si votre e-mail, téléphone ou compte n’est plus accessible.">{message?<div className="recovery-success"><LifeBuoy size={18}/><p>{message}</p></div>:<form onSubmit={submit}>{error&&<div className="form-alert">{error}</div>}<div className="field"><label>Nom et prénom</label><input className="input" name="name" required/></div><div className="field"><label>E-mail ou téléphone de contact</label><input className="input" name="contact" required/></div><div className="field"><label>Expliquez le problème</label><textarea className="textarea" name="message" required placeholder="E-mail oublié, ancien numéro, compte bloqué…"/></div><button className="btn btn-primary recovery-submit"><LifeBuoy size={14}/> Envoyer ma demande</button></form>}</RecoveryShell>}
