import{NextResponse}from"next/server";import{requireApiUser}from"@/lib/auth";import{db}from"@/lib/db";import{getFile}from"@/lib/storage";import{unauthorized}from"@/lib/http";type C={params:Promise<{id:string}>};export async function GET(_:Request,{params}:C){const u=await requireApiUser();if(!u)return unauthorized();const{id}=await params;const f=await db.communicationAttachment.findUnique({where:{id}});if(!f)return NextResponse.json({error:"Fichier introuvable."},{status:404});if(f.externalUrl)return NextResponse.redirect(f.externalUrl);if(!f.storageKey)return NextResponse.json({error:"Fichier indisponible."},{status:404});const b=await getFile(f.storageKey);return new NextResponse(new Uint8Array(b),{headers:{"Content-Type":f.mimeType||"application/octet-stream","Content-Disposition":`attachment; filename*=UTF-8''${encodeURIComponent(f.name)}`}})}
