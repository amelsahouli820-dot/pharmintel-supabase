import {NextRequest,NextResponse} from "next/server";import{z}from"zod";import{requireApiUser}from"@/lib/auth";import{db}from"@/lib/db";import{assertSameOrigin,audit,badRequest,clientIp,forbidden,unauthorized}from"@/lib/http";const types=["WHOLESALER","LABORATORY","HEALTH_ESTABLISHMENT","PHARMACY","SUPPLIER","COMPETITOR","OTHER"] as const;const schema=z.object({type:z.enum(types),name:z.string().trim().min(2).max(200),aliases:z.array(z.string().trim().min(1).max(100)).max(20).default([])});export async function GET(r:NextRequest){const u=await requireApiUser();if(!u)return unauthorized();const t=r.nextUrl.searchParams.get("type");const items=await db.referenceEntity.findMany({where:{active:true,...(t&&types.includes(t as any)?{type:t as any}:{})},orderBy:[{type:"asc"},{name:"asc"}]});return NextResponse.json({items})}export async function POST(r:NextRequest){if(!assertSameOrigin(r))return forbidden();const u=await requireApiUser("ADMIN");if(!u)return unauthorized();const p=schema.safeParse(await r.json().catch(()=>null));if(!p.success)return badRequest(p.error.issues[0]?.message||"Donnée invalide.");const item=await db.referenceEntity.create({data:p.data}).catch(()=>null);if(!item)return NextResponse.json({error:"Cette entrée existe déjà."},{status:409});await audit(u.id,"CATALOG_ENTRY_CREATED","ReferenceEntity",item.id,{type:item.type,name:item.name},clientIp(r));return NextResponse.json({item},{status:201})}
