"use client";import{FormEvent,useState}from"react";import{KeyRound,Mail}from"lucide-react";import{RecoveryShell}from"@/components/RecoveryShell";export default function Page(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/auth/recovery/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const d=await r.json();setLoading(false);if(!r.ok)return setError(d.error);setMessage(d.message)}return <RecoveryShell title="Mot de passe oublié" subtitle="Saisissez votre adresse e-mail professionnelle ou personnelle.">{message?<div className="recovery-success"><KeyRound size={18}/><p>{message}</p></div>:<form onSubmit={submit}>{error&&<div className="form-alert">{error}</div>}<div className="field"><label>Adresse e-mail</label><div className="input-icon"><Mail size={17}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoFocus/></div></div><button className="btn btn-primary recovery-submit" disabled={loading}>{loading?"Vérification…":"Envoyer le lien sécurisé"}</button><small className="safe-note">Le lien expire après 30 minutes et ne peut être utilisé qu’une fois.</small></form>}</RecoveryShell>}
