export type CommercialInput={priceHt?:number|null;discountPercent?:number|null;ugPercent?:number|null;offerBuyQty?:number|null;offerFreeQty?:number|null;offerText?:string|null};export function discountToUg(discount:number){if(!Number.isFinite(discount)||discount<0||discount>=100)return null;return Math.round(discount/(100-discount)*10000)/100}export function ugToDiscount(ug:number){if(!Number.isFinite(ug)||ug<0)return null;return Math.round(ug/(100+ug)*10000)/100}export function parseOffer(value:string|undefined|null){const m=value?.match(/(\d+)\s*\+\s*(\d+)/);if(!m)return null;const buy=Number(m[1]),free=Number(m[2]);return buy>0&&free>=0?{buy,free}:null}export function calculateCommercial(input:CommercialInput){const parsed=parseOffer(input.offerText);const buy=Number(input.offerBuyQty??parsed?.buy??0),free=Number(input.offerFreeQty??parsed?.free??0);let discount=input.discountPercent==null?null:Number(input.discountPercent),ug=input.ugPercent==null?null:Number(input.ugPercent);if(buy>0&&free>=0){ug=Math.round(free/buy*10000)/100;discount=ugToDiscount(ug)}else if(discount!=null&&discount<100)ug=discountToUg(discount);else if(ug!=null)discount=ugToDiscount(ug);const price=input.priceHt==null?null:Number(input.priceHt);const net=price==null?null:Math.round(price*(1-(discount||0)/100)*100)/100;const afterUg=net==null?null:Math.round(net*(100/(100+(ug||0)))*100)/100;const savings=price==null||afterUg==null?null:Math.round((price-afterUg)*100)/100;return{discountPercent:discount,ugPercent:ug,offerBuyQty:buy||null,offerFreeQty:buy?free:null,offerText:buy?`${buy}+${free}`:input.offerText||null,netPrice:net,priceAfterUg:afterUg,savings}}
